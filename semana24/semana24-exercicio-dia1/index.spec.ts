import { array } from './index';
import { resultTotal } from './index';


describe('Description', () => {
    it("", async () => {

        // array.slice(7, 1)
        console.log(array)
        // expect(resultTotal(array)).
    })
});